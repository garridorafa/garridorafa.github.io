import styled from "styled-components";
import { Segment } from "semantic-ui-react";

export const HeroSegment = styled(Segment)({
  padding: "8em 10em !important",
});

export const AvatarImage = styled.img`
  border-radius: 50%;
  object-fit: cover;
`;
